The glew files are the ones that you should have compiled if you followed the video
They should be placed in C:\Program Files (x86)\CodeBlocks\MinGW\lib

The compile.bat file is used to compile the glew files. You need to have it installed in the base glew directory as shown in the video. The compiler must be in the system path so that it can use it.
